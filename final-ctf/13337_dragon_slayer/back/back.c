#include <stdio.h>

int main()
{
	char s[] = "\bdef";
	printf("%s\n", s);
	/* char buf[128]; */
	/* sprintf(buf, "%s\n", s); */
	/* for(int i = 0; i < 16; ++i) */
	/* 	printf("%d %c\n", buf[i], buf[i]); */
}
